{
    "version": "0.2.0",
    "configurations": [
        {
            "useApiV1": false,
            "name": "Debug parser",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}",
            "cwd": "${workspaceFolder}",
        }
    ]
}
